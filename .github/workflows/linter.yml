name: Linting

on: [pull_request]

jobs:
  styles:
    runs-on: self-hosted
    name: Linting
    steps:
      - uses: actions/checkout@master
      - uses: actions/setup-python@v1
      - uses: whynothugo/python-linting@master
